package dev.jetpacker.core

import kotlin.test.Test
import kotlin.test.assertTrue

/** Proves the test toolchain is wired up; replace with real tests in Phase 1. */
class BuildSanityTest {
    @Test
    fun testInfrastructureWorks() {
        assertTrue(true)
    }
}
